{
  "name": "@web.worker/unstorage-drivers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow bun",
    "build": "bun build ./packages/memos/index.ts --outdir=dist --outfile=index.js -e unstorage --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0",
    "eslint": "^8.52.0",
    "eslint-config-unjs": "^0.2.1"
  },
  "dependencies": {
    "pino-pretty": "^13.0.0",
    "unstorage": "^1.15.0"
  }
}
