{
  "name": "@web.worker/unstorage-drivers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow bun",
    "build:xlog": "cd packages/unstorage-xlog-driver && bun run build",
    "build": "cd packages/unstorage-xlog-driver && bun run build",
    "lint": "cd packages/unstorage-xlog-driver && bun run lint",
    "test:types": "cd packages/unstorage-xlog-driver && tsc --noEmit --skipLibCheck",
    "test": "cd packages/unstorage-xlog-driver && bun test"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.12.0",
    "@modelcontextprotocol/inspector": "^0.10.2",
    "@types/bun": "^1.2.10",
    "@types/debug": "^4.1.12",
    "bun-plugin-dts": "^0.3.0",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "debug": "^4.4.0",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "unstorage": "^1.15.0",
    "zod": "^3.24.3"
  }
}
